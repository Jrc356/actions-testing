name: "Caller"

on:
  workflow_dispatch:
    inputs:
      service:
        type: text
        required: true

jobs:
  call-${{ inputs.service }}:
    steps:
      - run: echo ${{ inputs.service }}
